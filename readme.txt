Code details
 
1.     Before running the code, provide the root path in utils.py (refer to the comments at the top of the file)
 
2.     To run the code (pre-processing + random forest training) – execute train.py
 
3.     As soon as the pre-processing is finished, a new file will be created in “../code/data/” with the pre-processed data – trainingdata.py
